# 部署教程：让网站能「发链接」给别人打开

现在网站只在你电脑本地。要「发链接、别人一点就打开」，需要把它放到网上。下面从**最快上手**到**能上百度**，一步步来。

---

## 先看清楚：三种方案

| 方案 | 成本 | 国内打开速度 | 能发链接 | 百度能收录 |
|---|---|---|---|---|
| **A. Gitee Pages** | 免费 | 快 | ✅ | 部分 |
| **B. GitHub Pages** | 免费 | 较慢 | ✅ | 较难 |
| **C. 云服务器/OSS + 域名 + 备案** | 约 ¥100~150/年 | 最快 | ✅ | ✅ 完整 |

> 建议：先用 **方案 A** 拿到一个能发的链接，之后再走 **方案 C** 做百度收录。

---

## 方案 A：Gitee Pages（免费 · 国内快 · 10 分钟）

1. 注册并登录 [gitee.com](https://gitee.com)（需要实名认证）。
2. 点右上角「+」→「新建仓库」，仓库名填 `mufeng-blog`，选「公开」→ 创建。
3. 进入仓库，点「上传文件」，把 `mufeng-blog/` 文件夹里的文件**全部拖进去**上传（index.html、article.html、style.css、assets 文件夹、robots.txt、sitemap.xml、README、部署教程）。
4. 上传完，点仓库顶部菜单「服务」→「Gitee Pages」→「启动」。
5. 得到地址：`https://你的用户名.gitee.io/mufeng-blog/` —— **这就是你能发给别人的链接。**

> 注意：Gitee Pages 会校验网站内容，且如果绑定自定义域名，域名必须先备案。

## 方案 B：GitHub Pages（免费 · 国外快 · 国内能开但慢）

1. 注册并登录 [github.com](https://github.com)。
2. 新建仓库，名字填 `你的用户名.github.io`（必须这个名字），选 Public。
3. 仓库里「Add file」→「Upload files」，把项目文件全部拖进去 → Commit。
4. 仓库「Settings」→「Pages」→ Source 选 `main` 分支、根目录 → Save。
5. 得到地址 `https://你的用户名.github.io/`。

---

## 方案 C：云服务器/OSS + 域名 + 备案（百度收录的正道）

百度对「能搜到」的要求，基本等于：**国内服务器 + 备案 + 独立域名**。免费方案（A/B）能发链接，但百度收录差。

1. **买域名**：阿里云/腾讯云，约 ¥50~80/年。建议直接买「mufeng」相关的拼音域名（如 `mufeng.me` / `mufengblog.com`）。
2. **买空间**：二选一
   - 轻量应用服务器（约 ¥50~100/年），可搭 Nginx；
   - 或对象存储 OSS/COS 静态托管（更简单，纯静态站够用）。
3. **上传文件**：把项目文件传到服务器网页根目录（或 OSS 桶）。
4. **备案**：在云厂商控制台走「ICP 备案」，约 1~2 周（免费，必须做）。
5. **解析域名**：在域名控制台把域名 A 记录指向服务器 IP / CNAME 到 OSS。
6. 备案通过后，你的网站就是 `https://你的域名/`，可以开始做百度收录。

---

## 百度收录：让「沐枫」能被搜到

**第一步 · 换掉占位域名**
我把 `robots.txt`、`sitemap.xml`、`index.html` 里的 `你的域名` 都留成了占位。上线后把「你的域名」替换成真实域名。

**第二步 · 注册百度站长平台**
1. 打开 [ziyuan.baidu.com](https://ziyuan.baidu.com)，用百度账号登录。
2. 「添加站点」→ 填你的域名。
3. 验证：选「HTML 标签验证」，把给你的 `content` 值填进 `index.html` 里这行的引号中：
   ```html
   <meta name="baidu-site-verification" content="codeva-替换为你的验证码">
   ```
   （我已把这行预留好了，就在 `<head>` 里。）

**第三步 · 提交收录**
1. 站长平台 →「普通收录」→「sitemap」→ 提交 `https://你的域名/sitemap.xml`。
2. 「手动提交」→ 把首页 `https://你的域名/` 提交进去。
3. 也可以装「百度统计」，多一个被发现的入口。

**第四步 · 等 + 持续更新**
- 新站收录通常要**几天到几周**，急不来。
- 持续发原创文章（用 `article.html` 模板），收录会越来越多。

---

## 关于「百度搜『沐枫』排第一」，说几句实话

技术上我能做的已经做完了：
- ✅ 标题 / 描述 / 关键词（`<title>`、`<meta name="description/keywords">`）
- ✅ 语义化 HTML + H1 结构 + 图片 alt
- ✅ `sitemap.xml` + `robots.txt`
- ✅ 结构化数据 JSON-LD（人物身份）
- ✅ `canonical` 链接 + 百度站长验证预留位

但**百度排名前几，主要不靠这些代码**，靠的是：
1. **备案 + 国内主机**（没备案，基本搜不到）
2. **百度站长平台提交 + 验证**
3. **持续原创内容**（这是权重最大的因素）
4. **外链 / 被别的站引用**
5. **时间**（新站通常要几个月才稳定）

另外提醒：「沐枫」这个词如果已经被别的同名账号（尤其大 V / 企业）占位，你需要靠**更多原创内容 + 更精准的长尾词**（比如「沐枫 AI 发售」「沐枫 职业外脑」「沐枫 发售点金手」）先排上去，再逐步吃下「沐枫」这个词。

---

## 最快验证清单

- [ ] 用方案 A/B 拿到了一个能发给别人的链接
- [ ] 手机打开链接，确认能正常显示（图、字、二维码都在）
- [ ] （进阶）买了域名 + 备案，替换了所有「你的域名」占位
- [ ] （进阶）百度站长平台提交了 sitemap + 首页
